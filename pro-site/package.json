{
  "name": "media-downloader-pro-site",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "dev": "npm run build && npx wrangler@4.120.0 dev dist/server/index.js --assets dist/client",
    "test": "npm run build && node --test test.mjs"
  }
}
