<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="VideoEnhancer_Player.Pages.LibraryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="24,16,24,16" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Style="{StaticResource TitleTextBlockStyle}" Text="Library" />

        <CommandBar Grid.Row="1" DefaultLabelPosition="Right" IsOpen="False">
            <AppBarButton Click="AddVideo_Click" Label="Add video">
                <AppBarButton.Icon>
                    <SymbolIcon Symbol="Add" />
                </AppBarButton.Icon>
            </AppBarButton>
            <AppBarButton Click="AddFolder_Click" Label="Add folder">
                <AppBarButton.Icon>
                    <SymbolIcon Symbol="Folder" />
                </AppBarButton.Icon>
            </AppBarButton>
            <AppBarButton Click="Clear_Click" Label="Clear">
                <AppBarButton.Icon>
                    <SymbolIcon Symbol="Delete" />
                </AppBarButton.Icon>
            </AppBarButton>
        </CommandBar>

        <ListView
            x:Name="VideosList"
            Grid.Row="2"
            IsItemClickEnabled="False"
            SelectionMode="Single">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:LibraryVideoItem" xmlns:local="using:VideoEnhancer_Player">
                    <Grid Padding="0,10" ColumnSpacing="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="96" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <Border
                            Width="88"
                            Height="50"
                            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                            CornerRadius="4">
                            <Image Source="{x:Bind ThumbnailSource, Mode=OneWay}" Stretch="UniformToFill" />
                        </Border>
                        <StackPanel Grid.Column="1" Spacing="4">
                            <TextBlock Text="{x:Bind Title}" FontWeight="SemiBold" />
                            <TextBlock Text="{x:Bind Subtitle}" Opacity="0.72" TextTrimming="CharacterEllipsis" />
                            <TextBlock Text="{x:Bind Path}" Opacity="0.56" TextTrimming="CharacterEllipsis" />
                        </StackPanel>
                        <Button
                            Grid.Column="2"
                            Click="Open_Click"
                            Content="Open"
                            Tag="{x:Bind Path}" />
                        <Button
                            Grid.Column="3"
                            Click="Reveal_Click"
                            Content="Reveal"
                            Tag="{x:Bind Path}" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
